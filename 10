#include <iostream>
#include <vector>
using namespace std;

int main() {
    int m, n;
    cin >> m >> n;
    vector<vector<int>> mat(m, vector<int>(n));
    for (int i = 0; i < m; i++)
        for (int j = 0; j < n; j++)
            cin >> mat[i][j];

    vector<int> res;
    int prev = mat[0][0];
    vector<int> count(10001, 0);

    for (int j = 0; j < n; j++)
        count[mat[0][j]] = 1;

    for (int i = 1; i < m; i++)
        for (int j = 0; j < n; j++)
            if (count[mat[i][j]] == i)
                count[mat[i][j]]++;

    for (int i = 0; i <= 10000; i++)
        if (count[i] == m)
            cout << i << " ";

    return 0;
}
